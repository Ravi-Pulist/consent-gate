{
  "_description": "Slack MCP Server — Send messages, read channels for team notifications",
  "_used_by": ["derek-sm", "ravi-devops"],
  "_requires": ["SLACK_BOT_TOKEN", "SLACK_TEAM_ID"],
  "_note": "Used for sprint standup updates, deployment notifications, and CTO alerts",
  "mcpServers": {
    "slack": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-server-slack"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_TEAM_ID": "${SLACK_TEAM_ID}"
      }
    }
  }
}
